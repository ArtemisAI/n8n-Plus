# -- General Application Settings --
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO

# -- Database Configuration --
# For SQLite (default if PostgreSQL variables are not set)
DATABASE_URL_SQLITE=sqlite:///./n8n_templates.db

# For PostgreSQL (uncomment and fill these to use PostgreSQL)
# DATABASE_URL_POSTGRES=postgresql://user:password@host:port/dbname

# -- GitHub Template Fetcher (to be used in a later step) --
# GITHUB_REPO_URL_PERSONAL_TEMPLATES=https://github.com/yourusername/your-n8n-templates.git
# GITHUB_ACCESS_TOKEN=your_github_personal_access_token # If needed for private repos
